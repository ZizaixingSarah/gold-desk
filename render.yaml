services:
  - type: web
    name: gold-price-logic-desk
    runtime: node
    plan: free
    buildCommand: ""
    startCommand: "node server.js"
    healthCheckPath: /healthz
    envVars:
      - key: NODE_ENV
        value: production
